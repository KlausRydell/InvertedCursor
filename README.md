# InvertedCursor
A lightweight Unreal Engine runtime plugin that renders a Minecraft-style inverted crosshair by using a post-process material to invert the scene only where a cursor mask is present.
