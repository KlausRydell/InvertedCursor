# InvertedCursor
A lightweight Unreal Engine plugin that renders a **Minecraft-style inverted cursor or crosshair** using a post-process material.  The material inverts the scene beneath the cursor mask, helping it stand out against different backgrounds without requiring a UMG widget.
